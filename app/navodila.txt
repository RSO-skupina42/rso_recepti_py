zaženi app lokalno:

najprej zaženi docker za bazo
sudo docker run -d --name farecept -e POSTGRES_USER=dbuser -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=farecept-db -p 5432:5432 postgres:13

potem zaženi app 
 uvicorn main:app --host 0.0.0.0 --port 8003 --reload

 odpri browser na localhost:8003